<div class="knowledge-map" aria-hidden="true">
  <div class="map-meta">
    <span>Ecosistema técnico</span>
    <span class="map-state"><i></i> Conexiones activas</span>
  </div>

  <div class="map-canvas">
    <div class="orbit orbit-one"></div>
    <div class="orbit orbit-two"></div>

    <div class="network-line line-ai"><span></span></div>
    <div class="network-line line-automation"><span></span></div>
    <div class="network-line line-software"><span></span></div>
    <div class="network-line line-data"><span></span></div>
    <div class="network-line line-web"><span></span></div>

    <section class="knowledge-cluster cluster-ai">
      <p><i></i> IA aplicada</p>
      <div class="cluster-tools">
        <span>CrewAI</span><span>LLMs</span><span>RAG</span><span>MCP</span><span>Firecrawl</span><span>OpenCode</span>
      </div>
    </section>

    <section class="knowledge-cluster cluster-automation">
      <p><i></i> Automatización</p>
      <div class="cluster-tools">
        <span>n8n</span><span>Webhooks</span><span>REST APIs</span><span>ETL</span><span>Google Workspace</span><span>Notion</span>
      </div>
    </section>

    <div class="map-core">
      <span>Arquitectura</span>
      <strong>Sistemas</strong>
      <small>IA + software</small>
      <i></i>
    </div>

    <section class="knowledge-cluster cluster-software">
      <p><i></i> Software</p>
      <div class="cluster-tools">
        <span>Python</span><span>TypeScript</span><span>React</span><span>Node.js</span><span>Astro</span><span>Express</span><span>JWT</span>
      </div>
    </section>

    <section class="knowledge-cluster cluster-data">
      <p><i></i> Datos</p>
      <div class="cluster-tools">
        <span>PostgreSQL</span><span>MongoDB</span><span>Streamlit</span><span>httpx</span><span>BeautifulSoup</span>
      </div>
    </section>

    <section class="knowledge-cluster cluster-web">
      <p><i></i> Productos web</p>
      <div class="cluster-tools">
        <span>WordPress</span><span>WooCommerce</span><span>Elementor</span><span>PayPal</span><span>Tailwind</span><span>Markdown</span><span>YoastSEO</span><span>LiteSpeed</span>
      </div>
    </section>
  </div>

  <div class="map-footer">
    <span>Investigar</span><i></i><span>Conectar</span><i></i><span>Construir</span><i></i><span>Operar</span>
  </div>
</div>

<style>
  .knowledge-map {
    --map-bg: color-mix(in srgb, var(--bg) 84%, var(--accent-tint));
    --map-panel: color-mix(in srgb, var(--bg) 90%, transparent);
    --map-line: color-mix(in srgb, var(--line-strong) 80%, transparent);
    --map-line-soft: color-mix(in srgb, var(--line) 68%, transparent);
    --map-glow: color-mix(in srgb, var(--accent) 16%, transparent);
    position: relative;
    isolation: isolate;
    min-height: 482px;
    overflow: hidden;
    border: 1px solid var(--map-line);
    background:
      radial-gradient(circle at 50% 49%, var(--map-glow), transparent 31%),
      linear-gradient(var(--map-line-soft) 1px, transparent 1px),
      linear-gradient(90deg, var(--map-line-soft) 1px, transparent 1px),
      var(--map-bg);
    background-size: auto, 38px 38px, 38px 38px, auto;
    box-shadow:
      0 26px 70px color-mix(in srgb, var(--ink) 9%, transparent),
      inset 0 0 0 1px color-mix(in srgb, var(--bg) 52%, transparent);
    color: var(--ink);
    transition: border-color .3s ease, box-shadow .3s ease;
  }

  .knowledge-map::after {
    position: absolute;
    z-index: 6;
    inset: 9px;
    border: 1px solid color-mix(in srgb, var(--line) 42%, transparent);
    content: '';
    pointer-events: none;
  }

  .knowledge-map:hover {
    border-color: color-mix(in srgb, var(--accent) 55%, var(--line));
    box-shadow:
      0 30px 82px color-mix(in srgb, var(--ink) 12%, transparent),
      inset 0 0 0 1px color-mix(in srgb, var(--bg) 52%, transparent);
  }

  .map-meta,
  .map-footer {
    position: relative;
    z-index: 7;
    display: flex;
    align-items: center;
    color: var(--muted);
    font-size: .59rem;
    font-weight: 600;
    letter-spacing: .14em;
    text-transform: uppercase;
  }

  .map-meta {
    justify-content: space-between;
    min-height: 49px;
    padding-inline: 1.15rem;
    border-bottom: 1px solid var(--map-line);
    background: color-mix(in srgb, var(--map-panel) 78%, transparent);
    backdrop-filter: blur(8px);
  }

  .map-state {
    display: inline-flex;
    align-items: center;
    gap: .45rem;
    color: var(--accent-ink);
  }

  .map-state i {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent) 48%, transparent);
    animation: map-live 2.7s ease-out infinite;
  }

  .map-canvas {
    position: relative;
    z-index: 2;
    min-height: 386px;
  }

  .orbit {
    position: absolute;
    z-index: 0;
    top: 50%;
    left: 50%;
    border: 1px solid color-mix(in srgb, var(--accent) 20%, transparent);
    border-radius: 50%;
    pointer-events: none;
    transform: translate(-50%, -50%);
  }

  .orbit-one {
    width: 58%;
    aspect-ratio: 1;
    animation: orbit-breathe 8s ease-in-out infinite;
  }

  .orbit-two {
    width: 84%;
    aspect-ratio: 1;
    border-style: dashed;
    opacity: .48;
    animation: orbit-breathe 11s 1.8s ease-in-out infinite reverse;
  }

  .knowledge-cluster {
    position: absolute;
    z-index: 4;
    padding: .62rem .7rem;
    border: 1px solid var(--map-line);
    background: color-mix(in srgb, var(--map-panel) 91%, transparent);
    box-shadow: 0 8px 24px color-mix(in srgb, var(--ink) 6%, transparent);
    transition: border-color .3s ease, background .3s ease, transform .3s ease;
  }

  .knowledge-map:hover .knowledge-cluster {
    border-color: color-mix(in srgb, var(--accent) 37%, var(--map-line));
  }

  .knowledge-cluster p {
    display: flex;
    align-items: center;
    gap: .4rem;
    margin: 0 0 .42rem;
    color: var(--accent-ink);
    font-size: .52rem;
    font-weight: 600;
    letter-spacing: .12em;
    text-transform: uppercase;
  }

  .knowledge-cluster p i {
    width: 5px;
    height: 5px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--accent);
  }

  .cluster-tools {
    display: flex;
    flex-wrap: wrap;
    gap: .14rem .46rem;
  }

  .cluster-tools span {
    position: relative;
    color: var(--ink-soft);
    font-size: .55rem;
    line-height: 1.45;
    white-space: nowrap;
  }

  .cluster-tools span:not(:first-child)::before {
    position: absolute;
    top: 50%;
    left: -.29rem;
    width: 2px;
    height: 2px;
    border-radius: 50%;
    background: var(--muted);
    content: '';
  }

  .cluster-ai {
    top: 5%;
    left: 50%;
    width: 58%;
    transform: translateX(-50%);
  }

  .cluster-automation {
    top: 32%;
    left: 3%;
    width: 34%;
  }

  .cluster-software {
    top: 32%;
    right: 3%;
    width: 34%;
  }

  .cluster-data {
    bottom: 5%;
    left: 4%;
    width: 43%;
  }

  .cluster-web {
    right: 4%;
    bottom: 5%;
    width: 43%;
  }

  .knowledge-map:hover .cluster-ai {
    transform: translate(-50%, -2px);
  }

  .knowledge-map:hover .cluster-automation {
    transform: translateX(-2px);
  }

  .knowledge-map:hover .cluster-software {
    transform: translateX(2px);
  }

  .knowledge-map:hover .cluster-data {
    transform: translate(-2px, 2px);
  }

  .knowledge-map:hover .cluster-web {
    transform: translate(2px, 2px);
  }

  .map-core {
    position: absolute;
    z-index: 5;
    top: 51%;
    left: 50%;
    display: grid;
    width: 96px;
    min-height: 88px;
    place-content: center;
    border: 1px solid color-mix(in srgb, var(--accent) 58%, var(--map-line));
    background: color-mix(in srgb, var(--map-panel) 93%, var(--accent-tint));
    box-shadow:
      0 0 0 8px color-mix(in srgb, var(--bg) 45%, transparent),
      0 14px 34px color-mix(in srgb, var(--ink) 10%, transparent);
    text-align: center;
    transform: translate(-50%, -50%);
    transition: border-color .3s ease, box-shadow .3s ease, transform .3s ease;
  }

  .knowledge-map:hover .map-core {
    border-color: var(--accent);
    box-shadow:
      0 0 0 8px color-mix(in srgb, var(--bg) 45%, transparent),
      0 0 34px color-mix(in srgb, var(--accent) 22%, transparent);
    transform: translate(-50%, -53%);
  }

  .map-core span {
    color: var(--muted);
    font-size: .46rem;
    font-weight: 600;
    letter-spacing: .12em;
    text-transform: uppercase;
  }

  .map-core strong {
    margin-top: .18rem;
    font: 500 1rem/1.1 var(--font-display);
  }

  .map-core small {
    margin-top: .2rem;
    color: var(--muted);
    font-size: .53rem;
  }

  .map-core > i {
    position: absolute;
    top: .55rem;
    right: .55rem;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--accent);
  }

  .network-line {
    position: absolute;
    z-index: 1;
    overflow: visible;
    background: var(--map-line);
    transform-origin: 0 50%;
  }

  .network-line::before,
  .network-line::after {
    position: absolute;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--map-line);
    content: '';
  }

  .network-line::before {
    top: -2px;
    left: 0;
  }

  .network-line::after {
    top: -2px;
    right: 0;
  }

  .network-line span {
    position: absolute;
    top: -3px;
    left: 0;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 13px color-mix(in srgb, var(--accent) 80%, transparent);
    animation: map-signal 5.4s ease-in-out infinite;
  }

  .line-ai {
    top: 28%;
    left: 50%;
    width: 1px;
    height: 20%;
  }

  .line-ai::before,
  .line-ai::after {
    top: auto;
    right: auto;
    left: -2px;
  }

  .line-ai::before {
    top: 0;
  }

  .line-ai::after {
    bottom: 0;
  }

  .line-ai span {
    top: 0;
    left: -3px;
    animation-name: map-signal-y;
  }

  .line-automation {
    top: 49%;
    left: 37%;
    width: 13%;
    height: 1px;
  }

  .line-automation span {
    animation-delay: 1.15s;
  }

  .line-software {
    top: 49%;
    left: 50%;
    width: 13%;
    height: 1px;
  }

  .line-software span {
    animation-delay: 2.25s;
  }

  .line-data {
    top: 55%;
    left: 50%;
    width: 27%;
    height: 1px;
    transform: rotate(143deg);
  }

  .line-data span {
    animation-delay: 3.35s;
  }

  .line-web {
    top: 55%;
    left: 50%;
    width: 27%;
    height: 1px;
    transform: rotate(37deg);
  }

  .line-web span {
    animation-delay: 4.45s;
  }

  .map-footer {
    justify-content: center;
    gap: .5rem;
    min-height: 46px;
    padding-inline: .75rem;
    border-top: 1px solid var(--map-line);
    background: color-mix(in srgb, var(--map-panel) 78%, transparent);
  }

  .map-footer i {
    width: clamp(7px, 2vw, 17px);
    height: 1px;
    background: var(--map-line);
  }

  @keyframes map-signal {
    0%, 16% { left: 0; opacity: 0; }
    28%, 70% { opacity: 1; }
    84%, 100% { left: calc(100% - 7px); opacity: 0; }
  }

  @keyframes map-signal-y {
    0%, 16% { top: 0; opacity: 0; }
    28%, 70% { opacity: 1; }
    84%, 100% { top: calc(100% - 7px); opacity: 0; }
  }

  @keyframes map-live {
    0% { box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent) 48%, transparent); }
    70%, 100% { box-shadow: 0 0 0 8px color-mix(in srgb, var(--accent) 0%, transparent); }
  }

  @keyframes orbit-breathe {
    0%, 100% { opacity: .35; transform: translate(-50%, -50%) scale(.96); }
    50% { opacity: .72; transform: translate(-50%, -50%) scale(1.04); }
  }

  @media (prefers-color-scheme: dark) {
    .knowledge-map {
      --map-bg: color-mix(in srgb, var(--bg) 90%, #132337);
      --map-panel: color-mix(in srgb, var(--bg-alt) 90%, transparent);
      --map-line: color-mix(in srgb, var(--line-strong) 88%, transparent);
      --map-line-soft: color-mix(in srgb, var(--line) 76%, transparent);
      --map-glow: color-mix(in srgb, var(--accent) 13%, transparent);
      box-shadow:
        0 28px 74px rgba(0, 0, 0, .3),
        inset 0 0 0 1px rgba(255, 255, 255, .018);
    }
  }

  @media (max-width: 560px) {
    .knowledge-map {
      min-height: 460px;
      background-size: auto, 32px 32px, 32px 32px, auto;
    }

    .map-meta {
      min-height: 46px;
      padding-inline: .9rem;
      font-size: .5rem;
    }

    .map-canvas {
      min-height: 370px;
    }

    .knowledge-cluster {
      padding: .52rem .55rem;
    }

    .cluster-ai {
      width: 68%;
    }

    .cluster-automation,
    .cluster-software {
      width: 34%;
    }

    .cluster-data,
    .cluster-web {
      width: 44%;
    }

    .knowledge-cluster p {
      font-size: .44rem;
    }

    .cluster-tools {
      gap: .1rem .39rem;
    }

    .cluster-tools span {
      font-size: .48rem;
    }

    .map-core {
      width: 86px;
      min-height: 78px;
    }

    .map-core strong {
      font-size: .9rem;
    }

    .map-footer {
      min-height: 43px;
      gap: .35rem;
      font-size: .44rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .knowledge-map,
    .knowledge-cluster,
    .map-core {
      transition: none;
    }

    .map-state i,
    .orbit,
    .network-line span {
      animation: none;
    }

    .network-line span {
      opacity: .72;
    }
  }
</style>
