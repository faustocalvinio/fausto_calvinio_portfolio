---
import { siteConfig } from '../config/site';
import type { SeoImage, SeoProps, StructuredData } from '../lib/seo';
import { seoIds, toAbsoluteUrl } from '../lib/seo';

const {
  title,
  titleTemplate = true,
  description = siteConfig.seo.defaultDescription,
  canonical,
  image,
  type = 'website',
  pageSchemaType = 'WebPage',
  noindex = false,
  nofollow = false,
  publishedTime,
  modifiedTime,
  tags = [],
  prev,
  next,
  mainEntityId,
  structuredData = [],
} = Astro.props;

const siteUrl = Astro.site ?? new URL(siteConfig.url);
const canonicalUrl = new URL(canonical ?? Astro.url.pathname, siteUrl);
canonicalUrl.search = '';
canonicalUrl.hash = '';

const pageTitle = title
  ? titleTemplate
    ? siteConfig.seo.titleTemplate.replace('%s', title)
    : title
  : siteConfig.seo.defaultTitle;

const resolvedImage: Required<SeoImage> = {
  src: toAbsoluteUrl(image?.src ?? siteConfig.seo.defaultImage.src, siteUrl.href),
  alt: image?.alt ?? siteConfig.seo.defaultImage.alt,
  width: image?.width ?? siteConfig.seo.defaultImage.width,
  height: image?.height ?? siteConfig.seo.defaultImage.height,
  type: image?.type ?? siteConfig.seo.defaultImage.type,
};

const robots = noindex
  ? `noindex, ${nofollow ? 'nofollow' : 'follow'}`
  : `index, ${nofollow ? 'nofollow' : 'follow'}, max-image-preview:large, max-snippet:-1, max-video-preview:-1`;

const socialUrls = Object.values(siteConfig.social).map(({ url }) => url);
const customStructuredData = Array.isArray(structuredData)
  ? structuredData
  : [structuredData];

const baseStructuredData: StructuredData[] = [
  {
    '@type': 'Person',
    '@id': seoIds.person,
    name: siteConfig.author.name,
    givenName: siteConfig.author.givenName,
    familyName: siteConfig.author.familyName,
    jobTitle: siteConfig.author.jobTitle,
    description: siteConfig.description,
    url: siteConfig.url,
    image: toAbsoluteUrl(siteConfig.author.profileImage),
    email: siteConfig.author.email,
    address: {
      '@type': 'PostalAddress',
      addressCountry: {
        '@type': 'Country',
        name: siteConfig.author.countryName,
        identifier: siteConfig.author.countryCode,
      },
    },
    sameAs: socialUrls,
    knowsAbout: siteConfig.seo.topics,
  },
  {
    '@type': 'WebSite',
    '@id': seoIds.website,
    url: siteConfig.url,
    name: siteConfig.name,
    description: siteConfig.description,
    inLanguage: siteConfig.languageTag,
    author: { '@id': seoIds.person },
    publisher: { '@id': seoIds.person },
  },
  {
    '@type': pageSchemaType,
    '@id': `${canonicalUrl.href}#webpage`,
    url: canonicalUrl.href,
    name: pageTitle,
    description,
    inLanguage: siteConfig.languageTag,
    isPartOf: { '@id': seoIds.website },
    about: { '@id': seoIds.person },
    ...((mainEntityId || pageSchemaType === 'ProfilePage') && {
      mainEntity: {
        '@id': mainEntityId ?? seoIds.person,
      },
    }),
    primaryImageOfPage: {
      '@type': 'ImageObject',
      url: resolvedImage.src,
      contentUrl: resolvedImage.src,
      width: resolvedImage.width,
      height: resolvedImage.height,
      caption: resolvedImage.alt,
    },
  },
];

const jsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@graph': [...baseStructuredData, ...customStructuredData],
}).replace(/</g, '\\u003c');
---

<meta name="generator" content={Astro.generator} />
<meta name="description" content={description} />
<meta name="author" content={siteConfig.author.name} />
<meta name="robots" content={robots} />
<link rel="canonical" href={canonicalUrl} />
<link rel="alternate" hreflang={siteConfig.languageTag} href={canonicalUrl} />
<link rel="alternate" hreflang="x-default" href={canonicalUrl} />
{prev && <link rel="prev" href={new URL(prev, siteUrl)} />}
{next && <link rel="next" href={new URL(next, siteUrl)} />}
{socialUrls.map((url) => <link rel="me" href={url} />)}

<meta property="og:site_name" content={siteConfig.name} />
<meta property="og:locale" content={siteConfig.locale} />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={description} />
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:image" content={resolvedImage.src} />
<meta property="og:image:secure_url" content={resolvedImage.src} />
<meta property="og:image:type" content={resolvedImage.type} />
<meta property="og:image:width" content={resolvedImage.width} />
<meta property="og:image:height" content={resolvedImage.height} />
<meta property="og:image:alt" content={resolvedImage.alt} />
{type === 'profile' && <meta property="profile:first_name" content={siteConfig.author.givenName} />}
{type === 'profile' && <meta property="profile:last_name" content={siteConfig.author.familyName} />}
{type === 'article' && <meta property="article:author" content={siteConfig.url} />}
{type === 'article' && publishedTime && <meta property="article:published_time" content={publishedTime} />}
{type === 'article' && modifiedTime && <meta property="article:modified_time" content={modifiedTime} />}
{type === 'article' && tags.map((tag) => <meta property="article:tag" content={tag} />)}

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={resolvedImage.src} />
<meta name="twitter:image:alt" content={resolvedImage.alt} />

<title>{pageTitle}</title>
<script type="application/ld+json" set:html={jsonLd}></script>
